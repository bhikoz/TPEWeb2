<?php require_once("templates/header.phtml") ?>
<?php if($verify): ?>
    <?php require_once("templates/formJuego.phtml") ?>
<?php endif ?>

<div class="container-listado">
    <table class="table table-dark">
        <thead>
            <tr>
                <th>Título</th>
                <th>Género</th>
                <th>Año</th>
                <th>Sinopsis</th>
                <?php if ($verify): ?>
                    <th>Acciones</th>
                <?php endif ?>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($Juegos as $juego): ?>
                <tr>
                    <td>
                        <a href="mostrarDetalleJuego/<?= $juego->id_videojuego ?>">
                            <?= $juego->titulo ?>
                        </a>
                    </td>
                    <td>
                        <a href="mostrarJuegoXGenero/<?= $juego->id_genero ?>">
                            <?= isset($juego->genero_nombre) ? $juego->genero_nombre : $juego->id_genero ?>
                        </a>
                    </td>
                    <td><?= $juego->anio_publicacion ?></td>
                    <td><?= $juego->sinopsis ?></td>
                    <?php if ($verify): ?>
                        <td>
                            <a href="eliminarJuego/<?= $juego->id_videojuego ?>" class="btn btn-danger">Eliminar</a>
                            <a href="formModificarJuego/<?= $juego->id_videojuego ?>" class="btn btn-success">Modificar</a>
                        </td>
                    <?php endif ?>
                </tr>
            <?php endforeach ?>
        </tbody>
    </table>
</div>